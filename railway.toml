[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn secretary_clean.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/bootstrap/status"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
